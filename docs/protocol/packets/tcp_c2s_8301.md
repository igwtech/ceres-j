# `TCP C->S 0x8301` — AuthB

**Transport:** TCP  
**Direction:** C->S  
**Identifier:** `0x8301`  
**Status:** verified  

## Evidence

<!-- catalog-evidence (auto-generated; do not edit manually — regenerate via `tools/catalog_extract.py --update-evidence`) -->

- Total observations: **21**
- Captures with this packet: **17/17**
- Size (bytes): min **53**, avg **53**, max **53**
- Per-capture counts:
  - `RETAIL_RETAIL_VEHICLE_DRONE_20260503_141715` × 3
  - `RETAIL_RETAIL_LONG_PARTY_A_20260503_130137` × 2
  - `RETAIL_CREATION_LEVELING_LONG_20260502_160841` × 2
  - `RETAIL_PLAZA_TO_PEPPER_CROSS_DISTRICT_20260502_103513` × 1
  - `RETAIL_FULL_PCAP_TRACE_20260426_154648` × 1
  - `RETAIL_ODA_20260426_202428` × 1
  - `RETAIL_RETAIL_CHARDEL_SUBWAY_20260503_132639` × 1
  - `RETAIL_AUGUSTO_20260426_201952` × 1
  - `RETAIL_RETAIL_LONG_PARTY_B_20260503_130343` × 1
  - `RETAIL_DRSTONE4_20260501_193336` × 1
  - `RETAIL_DRSTONE3_20260501_181349` × 1
  - `RETAIL_ZONING_AND_ITEMS_LONG_20260502_010613` × 1
  - `RETAIL_DRSTONE_20260501_172522` × 1
  - `RETAIL_HANNIBAL_20260426_201501` × 1
  - `RETAIL_NORMAN_20260426_200458` × 1
  - `RETAIL_DRSTONE_20260501_175315` × 1
  - `RETAIL_CASH_VENDOR_PCAP_FRESH_20260426_143918` × 1

Samples (first 32 bytes inner data):

```
#1: 8301ac1be9d100000000ff790b4108e958cb03000000120009006d736e32776f
```
```
#2: 8301ac1be9b800000000f467ec767cabc43600000000120009006d736e33776f
```
```
#3: 8301ac1be9b800000000142a7a8756371a8e00000000120009006d736e32776f
```

<!-- /catalog-evidence -->

## Structure

TCP C→S 0x8301 — **ResumeAuth** (= stage 3 char-selection +
session resume on a fresh TCP connection to GameServer:12000).
Triggers `UDPServerData` + `Location` handover. Live-verified
2026-05-22 against retail with msn3wolf account.

```
[0..1]    83 01                    TCP opcode
[2..5]    session_tag 4B           ac 1b e9 XX where XX varies
                                    slowly per build (0xb8 April,
                                    0xcf-d2 May 2026). Current
                                    retail seems lenient — `0xd2`
                                    accepted on 2026-05-22.
[6..9]    reserved 4B              zero — was labelled "client_port"
                                    historically but retail captures
                                    always show zeros here
[10]      enc_key K u8             = salt[0] (= K used for password)
[11..17]  salt[1..7] 7B            random per session — fresh rand
                                    state (separate from the main
                                    MSVCRT stream of stages 1+2)
[18..21]  char_spot LE32           **the actual char slot** (0..3 for
                                    msn3wolf's 4 chars: Krafteo /
                                    Norman / Hannibal / Augusto).
                                    Invalid slot → retail returns
                                    `0x83/0x03` ASCII error "Char
                                    does not exist on this server".
[22..23]  password_len LE16        = 2 × char count = 18
[24..25]  username_len LE16        includes NUL = 9
[26..N]   username CString         e.g. "msn3wolf\0"
[N+1..]   password encrypted       same encoder as Auth (0x8480),
                                    K = salt[0]
```

Sample retail bytes:
```
83 01  ac1be9d1  00000000  ff790b41  08  e958cb03000000  12 00 09 00
                                                          ^pwLen ^uLen
       ^reserved  ^cli_port=0x410b79ff  ^key=08
83 01 ac 1b e9 d1 00 00 00 00 ff 79 0b 41 08 e9 58 cb 03 00 00 00
12 00 09 00 6d 73 6e 32 77 6f ...
                  ^"msn2wo..."
```

The 4-byte `client_port` field at [6..9] is the LITTLE-ENDIAN
LE32 of the client's UDP listener port — not just a 16-bit
value. `0x410b79ff` = ~1090631679 in decimal; the high bits
likely encode a flag.

## Variants

Single 53-byte form. Field content varies per session and
per-char selection:
- `enc_key`: per-attempt random.
- `char_spot`: which of the 4 slots was selected (0..3 typical
  but the field carries an LE32 numeric key, not just an
  index).
- Username/password: per credentials.

The 4-byte `reserved` at [2..5] and 7-byte `reserved` at
[11..17] are not validated server-side — opaque session data.

## Observed contexts

Emitted exactly once per character selection — after the
player picks a char from the list (TCP S→C 0x8385) the client
emits 0x8301 to authenticate the choice. 21 emissions / 17
captures = ~1.2 per session.

## Open questions

- `client_port LE32` at [6..9]: the LE32 = 0x410b79ff for
  AUGUSTO. Stripped of high bits, port = 0x79ff = 31231 — a
  valid ephemeral UDP port. The high byte 0x41 may be a flag
  bit. Currently `AuthB.java` reads and ignores this field.
- `char_spot` at [18..21]: documented in source as the slot
  index but stored as an LE32 — possibly a hash / token tying
  the spot to a specific CharList entry, preventing replay
  attacks.

## Server-side handler

`server.gameserver.packets.client_tcp.AuthB` — parses
encryption key, decodes username + password, authenticates,
and emits the post-AuthB sequence:

1. Allocate per-session UDP port via `UdpPortPool.allocate()`.
2. Start `PlayerUdpListener` on the allocated port.
3. Emit `UDPServerData` (0x8305) with the UDP descriptor.
4. Emit `Packet830D` (0x830d, GameinfoReady).
5. Emit `Location` (0x830c) for the spawn zone.

Bounds-checked: usernameLength ∈ (0, 256], passwordLength ∈
[0, 256]. On failure: emits `RequestFailed("ERROR")`.

The 4-byte `client_port` and 7-byte reserved fields are
read-but-ignored. Authentication uses
`AccountManager.authenticate(username, password)` →
`AuthOutcome.OK` to advance.

