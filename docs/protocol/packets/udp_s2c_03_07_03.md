# `UDP S->C 0x03/0x07/0x03` — Multipart/Multipart-disc3 (?)

**Transport:** UDP  
**Direction:** S->C  
**Identifier:** `0x03/0x07/0x03`  
**Status:** partial  

## Evidence

<!-- catalog-evidence (auto-generated; do not edit manually — regenerate via `tools/catalog_extract.py --update-evidence`) -->

- Total observations: **1**
- Captures with this packet: **1/17**
- Size (bytes): min **1908**, avg **1908**, max **1908**
- Multipart: 2 fragments reassembled into 1 logical packet(s); avg 2 fragments per packet
  - Fragment size (bytes): min 919, max 1011
  - Chain keys observed: 0x00×1
- Per-capture counts:
  - `RETAIL_CREATION_LEVELING_LONG_20260502_160841` × 1

Samples (first 32 bytes inner data):

```
#1: 000002000000037407000021170c00010007004d61696c436f6e74726f6c0007
```

<!-- /catalog-evidence -->

## Structure

UDP S→C 0x03/0x07/0x03 — **Multipart with discriminator 0x03**.
Single 1908-byte reassembled body across 2 fragments. The
sample's ASCII content includes "MailControl" — likely the
mail/inbox subsystem state.

```
Per-fragment header (6B per multipart_framing.md):
  [0x00] chain_key
  [0x03] discriminator (NEW — not CharInfo / not CharsysInfo)
  [total_size LE32]
[fragment payload]
```

Sample inner (first 32B):
```
00 00 02 00 00 00 03 74 07 00 00 21 17 0c 00 01 00 07 00 4d 61 69 6c 43 6f 6e 74 72 6f 6c 00 07
                                        ^? ^?  ^? ^? ^? "MailControl\0"
```

ASCII "MailControl" suggests this multipart channel carries
**mail / messaging state** — inbox listing, mail content, or
notifications.

## Variants

Single 1908B reassembled sample (2 fragments × ~919/1011B).

## Observed contexts

CREATION_LEVELING_LONG only — single emission. Likely an
inbox-refresh or mail-arrival burst.

## Open questions

- Discriminator 0x03 is not in the canonical
  `multipart_framing.md` discriminator map (which lists 0x01
  CharInfo, 0x02 CharsysInfo). Need to add this to the
  `MULTIPART_DISC_NAMES` table in `catalog_extract.py`.
- ASCII "MailControl" + the long body suggests serialized
  mail data. Format unknown.

## Server-side handler

Not specifically handled. Multipart reassembly logic catches
the chain, but the discriminator-0x03 routing has no
consumer. **Low priority** parity gap (1 retail sample); add
when implementing the mail subsystem.

